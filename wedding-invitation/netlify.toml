[build]
  base    = "wedding-invitation"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing — serve index.html for all routes so deep-links don't 404
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
